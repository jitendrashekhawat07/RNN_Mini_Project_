RNN-Based Sentiment Analysis using PyTorch

A Deep Learning NLP project that classifies text reviews into sentiment categories using a custom Recurrent Neural Network (RNN) implemented in PyTorch.

The project includes complete text preprocessing, TF-IDF feature extraction, model training, evaluation, and prediction pipeline.
